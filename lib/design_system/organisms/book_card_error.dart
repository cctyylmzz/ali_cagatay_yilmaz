import 'package:flutter/material.dart';

import '../molecules/book_poster_error.dart';

class BookCardError extends StatelessWidget {
  const BookCardError({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: BookPosterError());
  }
}
